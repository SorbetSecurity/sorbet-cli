[project]
name = "polyglot-py"
version = "1.0.0"
dependencies = [
  "requests>=2.31",
  "colorama>=0.4; platform_system == 'Windows'"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
