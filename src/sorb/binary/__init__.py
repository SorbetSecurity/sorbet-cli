"""Binary analysis subsystem: embedded ground-truth readers
(``sorb.binary.embedded``: Go buildinfo, cargo-auditable, .NET deps.json,
minimal ELF/PE section access) plus the full format-adapter and
fingerprinting layer."""
